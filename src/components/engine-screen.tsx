import { Text, View } from 'react-native';

export default function EngineScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', padding: 24 }}>
      <Text>Notification Alarm is Android-only.</Text>
    </View>
  );
}
